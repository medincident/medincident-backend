package classifier

// Aggregate-type strings written into every outbox envelope emitted
// from this package.
const (
	AggregateTypeIncidentCategory = "incident_category"
	AggregateTypeIncidentType     = "incident_type"
)
