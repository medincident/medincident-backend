version: v2

# Generates a single merged OpenAPI v2 spec from the query-side proto
# tree under api/proto/query. Output lives at
# api/openapi/query-server.swagger.json.

managed:
  enabled: true
  override:
    - file_option: go_package_prefix
      value: github.com/medincident/medincident-command-service/pkg
  disable:
    - file_option: go_package_prefix
      module: buf.build/googleapis/googleapis
    - file_option: go_package_prefix
      module: buf.build/grpc-ecosystem/grpc-gateway
    - file_option: go_package_prefix
      module: buf.build/protocolbuffers/wellknowntypes

inputs:
  - directory: api/proto
    paths:
      - api/proto/query

plugins:
  - local: [go, tool, protoc-gen-openapiv2]
    out: api/openapi
    strategy: all
    opt:
      - allow_merge=true
      - merge_file_name=query-server
